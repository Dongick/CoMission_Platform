file:
  upload:
    authentication:
      directory: ${AUTHENTICATION_FILE_UPLOAD}
    mission:
      directory: ${MISSION_FILE_UPLOAD}

spring:
  jpa:
    hibernate:
      ddl-auto: update
      format_sql: true
      show-sql: true